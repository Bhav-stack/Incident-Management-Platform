{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "palette-classic" },
          "custom": { "drawStyle": "line", "fillOpacity": 12, "lineWidth": 2, "showPoints": "never" },
          "unit": "short"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list", "placement": "bottom", "showLegend": true }, "tooltip": { "mode": "multi", "sort": "desc" } },
      "targets": [
        { "expr": "sum(rate(aegis_events_emitted_total[5m])) by (type)", "legendFormat": "emitted {{type}}", "refId": "A" },
        { "expr": "sum(rate(aegis_events_received_total[5m]))", "legendFormat": "received (ingest)", "refId": "B" },
        { "expr": "sum(rate(aegis_events_deduplicated_total[5m]))", "legendFormat": "deduplicated", "refId": "C" }
      ],
      "title": "Event pipeline throughput",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "thresholds" },
          "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": null }, { "color": "red", "value": 1 } ] },
          "unit": "short"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 6, "x": 12, "y": 0 },
      "id": 2,
      "options": { "colorMode": "value", "graphMode": "area", "justifyMode": "auto", "orientation": "auto", "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false } },
      "targets": [ { "expr": "sum(increase(aegis_anomalies_emitted_total[1h]))", "legendFormat": "anomalies (1h)", "refId": "A" } ],
      "title": "Confirmed anomalies",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "palette-classic" },
          "custom": { "drawStyle": "line", "fillOpacity": 12, "lineWidth": 2, "showPoints": "never" },
          "unit": "short"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 6, "x": 18, "y": 0 },
      "id": 3,
      "options": { "legend": { "displayMode": "list", "placement": "bottom", "showLegend": true }, "tooltip": { "mode": "multi", "sort": "desc" } },
      "targets": [
        { "expr": "sum(increase(aegis_incidents_opened_total[1h]))", "legendFormat": "opened (1h)", "refId": "A" },
        { "expr": "sum(increase(aegis_incidents_resolved_total[1h]))", "legendFormat": "resolved (1h)", "refId": "B" }
      ],
      "title": "Incidents opened vs resolved",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "palette-classic" },
          "custom": { "drawStyle": "line", "fillOpacity": 12, "lineWidth": 2, "showPoints": "never" },
          "unit": "short"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 4,
      "options": { "legend": { "displayMode": "list", "placement": "bottom", "showLegend": true }, "tooltip": { "mode": "multi", "sort": "desc" } },
      "targets": [
        { "expr": "sum(increase(aegis_proposals_created_total[1h]))", "legendFormat": "created", "refId": "A" },
        { "expr": "sum(increase(aegis_proposals_approved_total[1h]))", "legendFormat": "approved (human)", "refId": "B" },
        { "expr": "sum(increase(aegis_proposals_auto_approved_total[1h]))", "legendFormat": "auto-approved", "refId": "C" },
        { "expr": "sum(increase(aegis_proposals_rejected_total[1h]))", "legendFormat": "rejected", "refId": "D" },
        { "expr": "sum(increase(aegis_proposals_expired_total[1h]))", "legendFormat": "expired", "refId": "E" }
      ],
      "title": "Approval gate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "thresholds" },
          "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": null }, { "color": "orange", "value": 1 }, { "color": "red", "value": 3 } ] },
          "unit": "short"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 6, "x": 12, "y": 8 },
      "id": 5,
      "options": { "colorMode": "value", "graphMode": "area", "justifyMode": "auto", "orientation": "auto", "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false } },
      "targets": [
        { "expr": "sum(increase(aegis_actions_executed_total[1h]))", "legendFormat": "executed (1h)", "refId": "A" },
        { "expr": "sum(increase(aegis_actions_verified_total[1h]))", "legendFormat": "verified (1h)", "refId": "B" },
        { "expr": "sum(increase(aegis_actions_verification_failed_total[1h]))", "legendFormat": "verify failed (1h)", "refId": "C" },
        { "expr": "sum(increase(aegis_actions_rolled_back_total[1h]))", "legendFormat": "rolled back (1h)", "refId": "D" }
      ],
      "title": "Recovery actions",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "thresholds" },
          "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": null }, { "color": "orange", "value": 1 }, { "color": "red", "value": 5 } ] },
          "unit": "short"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 6, "x": 18, "y": 8 },
      "id": 6,
      "options": { "colorMode": "value", "graphMode": "area", "justifyMode": "auto", "orientation": "auto", "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false } },
      "targets": [ { "expr": "sum(increase(aegis_actions_escalated_total[1h]))", "legendFormat": "escalated (1h)", "refId": "A" } ],
      "title": "Escalations",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "palette-classic" },
          "custom": { "drawStyle": "line", "fillOpacity": 12, "lineWidth": 2, "showPoints": "never" },
          "unit": "short"
        },
        "overrides": [
          { "matcher": { "id": "byName", "options": "total lag" }, "properties": [ { "id": "custom.lineWidth", "value": 3 } ] }
        ]
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 16 },
      "id": 7,
      "options": { "legend": { "displayMode": "list", "placement": "bottom", "showLegend": true }, "tooltip": { "mode": "multi", "sort": "desc" } },
      "targets": [
        { "expr": "sum(aegis_kafka_lag) by (group, topic)", "legendFormat": "{{group}} / {{topic}}", "refId": "A" },
        { "expr": "sum(aegis_kafka_lag)", "legendFormat": "total lag", "refId": "B" }
      ],
      "title": "Kafka consumer lag (committed vs log end)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "thresholds" },
          "thresholds": { "mode": "absolute", "steps": [ { "color": "green", "value": null }, { "color": "orange", "value": 100 }, { "color": "red", "value": 1000 } ] },
          "unit": "short"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 6, "x": 12, "y": 16 },
      "id": 8,
      "options": { "colorMode": "value", "graphMode": "area", "justifyMode": "auto", "orientation": "auto", "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false } },
      "targets": [ { "expr": "sum(aegis_kafka_lag)", "legendFormat": "total lag", "refId": "A" } ],
      "title": "Total Kafka lag",
      "type": "stat"
    }
  ],
  "refresh": "10s",
  "schemaVersion": 39,
  "tags": ["aegis"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "Aegis · Incident platform",
  "uid": "aegis",
  "version": 1
}